# Weather Agent API

A FastAPI-based weather backend with structured tool architecture.

## Features

- REST API built with FastAPI
- Structured weather lookup
- Input normalization
- Swagger documentation
- Developer-controlled agent architecture

## Run Locally

```bash
pip install -r requirements.txt
python -m uvicorn app:app --reload
